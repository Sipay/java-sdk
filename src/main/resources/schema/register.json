{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Register",
  "description": "Send a request of register to Sipay",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "pattern": "^[\\w-]{6,128}$"
    }
  },
  "required": [
    "token"
  ]
}